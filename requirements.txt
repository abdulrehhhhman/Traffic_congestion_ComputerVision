streamlit>=1.28.0
opencv-python>=4.8.0
numpy>=1.24.0
ultralytics>=8.0.0
shapely>=2.0.0
Pillow>=9.0.0
matplotlib>=3.6.0
torch>=2.0.0
torchvision>=0.15.0
imageio>=2.25.0
imageio-ffmpeg>=0.4.8

# For JSON handling (usually built-in but explicit for clarity)
# json - built-in module

# For GUI components (usually built-in but might need explicit install on some systems)
# tkinter - usually built-in with Python

# Optional: For better performance
psutil>=5.9.0
